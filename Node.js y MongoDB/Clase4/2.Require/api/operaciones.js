const suma = (a,b) => a + b
const resta = (a,b) => a - b
const mult = (a,b) => a * b
const div = (a,b) => a / b
const mod = (a,b) => a % b

//export CommonJS

module.exports = {
  suma,
  resta,
  mult,
  div,
  mod
}